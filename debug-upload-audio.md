[OPEN] upload-audio

## 症状
- 用户反馈无法上传音频。

## 当前已知信息
- 项目为 Vue 3 + Express + MySQL + 阿里云 OSS。
- 近期已完成 OSS 私有桶、直传链路、MySQL 迁移。

## 初始假设
1. 前端上传接口请求参数或文件字段名与后端接口约定不一致，导致请求被拒绝。
2. 前端拿到的 OSS 直传签名无效，或直传请求头与签名时声明的 `Content-Type` 不一致。
3. 后端在生成上传目标或落库时，因为鉴权、必填字段或数据库约束失败而返回错误。
4. OSS 权限、Bucket 路径或对象前缀配置异常，导致音频上传阶段返回 403/4xx。
5. 上传成功但后续“提交 beat 记录”失败，表面看起来像“无法上传音频”。

## 计划
- 先阅读上传相关前后端代码，定位关键链路和可插桩点。
- 仅增加调试埋点，复现后基于日志判断根因。

## 证据
- 调试环境中未采集到浏览器日志，说明本次失败发生时前端未命中新增埋点，或使用的是旧前端构建。
- 数据库验证显示：当 `beats.uploaded_by = 13` 时，插入会触发 `fk_beats_uploaded_by` 外键错误。
- 数据库验证显示：当 `beats.uploaded_by = 1` 时，相同插入可以通过。
- 当前数据库仅保留 `admin`，且其 `id = 1`；此前会话中的 `admin` 登录态很可能仍携带旧 token（`id = 13`）。

## 初步结论
- 根因高度疑似为“浏览器保留了旧 token，token 中的用户 ID 已失效”，导致上传最后一步落库时把 `uploaded_by=13` 写入数据库并报错。

## 修复
- 在 `requireAuth` / `requireAdmin` 中增加数据库存在性校验与身份回捞。
- 若 token 中的用户 ID 已不存在，则优先按邮箱/用户名回捞当前用户，兼容“数据库清理后重新排 ID”的旧 token。
- 若回捞失败，才返回 `401`，避免继续执行到业务写入时报 `500`。
- 根据用户最新截图，确认浏览器到 OSS 的 `PUT` 预检被 CORS 拦截。
- 新增前端回退策略：若直传 OSS 失败，则自动改走后端 multipart 上传，由服务端再写入 OSS。
- 新增本地开发保护：当请求源为 `localhost/127.0.0.1` 时，后端不再下发 OSS 直传目标，直接让前端走 multipart 上传，彻底避开本地开发时的 OSS CORS 问题。

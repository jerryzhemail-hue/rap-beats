[OPEN] audio-3s-playback

## 症状
- 用户反馈：歌曲上传成功，但播放时只能播放 3 秒左右。

## 当前已知信息
- 项目使用 Vue 3 + Express + MySQL + 阿里云 OSS。
- 近期刚完成 OSS 浏览器直传与私有桶签名访问链路。
- 上传问题已基本修通，目前进入“播放受限”阶段。

## 初始假设
1. 上传到 OSS 的对象内容不完整，只保存了音频前几秒。
2. 播放接口返回的签名地址、响应头或鉴权方式导致浏览器只拉取到部分字节。
3. 后端播放链路没有正确支持 `Range` 请求，导致音频元素分段拉流异常。
4. 数据库存储的音频地址不正确，播放的并不是刚上传的完整文件。
5. 前端播放器或浏览器在鉴权/预加载失败后提前中断，表面看起来像只能播 3 秒。

## 计划
- 先阅读上传、播放、资源签名和前端播放器链路。
- 增加最小埋点，收集上传结果、播放 URL、请求头和播放事件证据。

## 证据
- 后端日志显示，播放请求命中 `beatId=33`，`userId=1`，`role=admin`，但 `vipLevel=free`。
- 后端连续多次进入 `remote stream served as preview`，说明当前账号走的是“试听模式”而不是完整版播放。
- 多次后续请求都携带 `Range: bytes=491520-`，说明浏览器尝试继续拉取试听后面的分段。
- 前端播放器日志显示 `duration=185.1298`，`currentTime=3.290978` 时触发 `errorCode=2`，不是音频本身只有 3 秒。

## 结论
- 根因不是文件只有 3 秒，而是远程 OSS 试听流对后续 `Range` 请求处理不正确，导致播放器在约 3.29 秒时出错中断。
- 当前 `admin` 账号的 `vip_level` 仍为 `free`，所以它本来就会走试听模式；但试听模式本应是约 30 秒，不应该 3 秒就停。

## 修复
- 在远程试听代理里正确解析并裁剪 `Range` 请求到试听字节上限内。
- 当浏览器请求超出试听范围时，返回 `416`，而不是反复把 `0-491519` 那一段错误地当作后续分段返回。
- 试听模式下将 `Accept-Ranges` 改为 `bytes`，让浏览器按正确的分段方式读取试听内容。
